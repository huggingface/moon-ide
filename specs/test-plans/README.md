# Test plans

**Outdated — do not create or modify.**

The files in this directory are a historical record from the project's early phases. They are no longer maintained: don't write new plans, don't edit existing ones, and don't reference them as a current source of truth. Intent now lives in ADRs ([specs/decisions/](../decisions/)) and roadmap entries; the commit body + `git log -p` covers the rest.

Existing plans are kept only as a record of what was true at the time they were written.
