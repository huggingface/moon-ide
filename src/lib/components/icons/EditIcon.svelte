<script lang="ts">
	// Pencil outline. Used by the coder transcript's per-user-row
	// "edit & resend" action — drops the prompt back into the
	// composer for the user to tweak before relaunching the turn.

	type Props = { size?: number };
	let { size = 14 }: Props = $props();
</script>

<svg
	xmlns="http://www.w3.org/2000/svg"
	width={size}
	height={size}
	viewBox="0 0 16 16"
	fill="none"
	stroke="currentColor"
	stroke-width="1.4"
	stroke-linecap="round"
	stroke-linejoin="round"
	aria-hidden="true"
	focusable="false"
>
	<path d="M11.5 2.5a1.4 1.4 0 0 1 2 2L5 13l-3 1 1-3 8.5-8.5Z" />
	<path d="M10.5 3.5l2 2" />
</svg>
